create table Students(Rno int primary key,
                     StudentName varchar (50),
					 Gender varchar (50),
					 Hobby varchar (50),
					 City varchar (50),
					 Dob varchar(50) )
select * from Students
